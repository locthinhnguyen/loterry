{
  "name": "my-loterry",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --experimental-specifier-resolution=node -r dotenv/config server.js",
    "dev": "nodemon --experimental-specifier-resolution=node -r dotenv/config --no-warnings server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mogoose": "^0.0.1-security",
    "mongoose": "^8.0.3",
    "nodemon": "^3.0.2"
  }
}
